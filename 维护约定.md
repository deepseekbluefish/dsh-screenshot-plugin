# 维护约定（Maintenance Notes）

> 2026-08-21 起生效，供后续修改本插件的 DeepSeek 参考。

## 备份与版本

- **v1.0.0**（当前可用基线）保存在：
  - 本地备份包：`dsh-screenshot-plugin-v1.0.0-备份-20260821.zip`（与插件同级的 DS 工作文件夹）
  - Git 标签 `v1.0.0`（本地 + GitHub 仓库）
  - GitHub 主仓库 main 分支
- 每个稳定版本打一个新 Git 标签（v1.1.0、v1.2.0……），便于随时回退。

## 用户要求的修改方式

1. **优先保证修改效率**——大胆改，不必因怕改坏而缩手缩脚。
2. 改坏了**不必立即回滚**；只有当问题实际影响使用时，才用备份恢复
   （`git revert` / `git checkout v1.0.0` / 用备份 zip 覆盖插件目录）。
3. 每次动手改代码前先 `git commit` 当前状态。

## 工程红线（改代码时不要碰）

- `lib/capture.ps1` 必须保持**纯 ASCII**（Windows PowerShell 5.1 会把无 BOM UTF-8
  按 ANSI 解析，多字节字符可能吞换行、把代码行吃进注释）。中文一律用 `[char]` 码构造。
- `lib/capture.ps1` 的结果 JSON 必须从脚本**主流程** `Write-Output`——写在 WinForms
  事件处理器里的输出会被引擎丢弃。
- `cordis.patch.yml` 顶层必须是 YAML **数组**（全注释空文件会触发加载报错）。
- 客户端代码改动需要**重启 DSH** 才生效；宿主路由与截屏脚本每次点击都是新进程读取，即时生效。
- 标记格式保持纯 ASCII `[Shot N HH:mm]`（对应文件 `截屏N_HHmm.png`）。
- 计数器文件 `.screenshot-counter.txt` 在截图保存目录，与插件代码分离，恢复插件不影响编号。

## 发布状态

- 等待 awesome-dsh-plugin 的 24 小时仓库年龄门槛（2026-08-22T06:35:54Z 之后可提 PR）。
- fork 分支 `add/dsh-screenshot-plugin` 已含规范条目与重新生成的 README，届时直接 `gh pr create`。
